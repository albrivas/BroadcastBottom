<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="SignUpLink" parent="Widget.AppCompat.TextView">
        <item name="android:autoLink">web</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="fontFamily">@font/ubuntu_medium</item>
        <item name="android:text">@string/label_login</item>
    </style>

    <style name="ForgotPassword" parent="Widget.AppCompat.TextView">
        <item name="android:autoLink">web</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="fontFamily">@font/ubuntu_medium</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:text">@string/label_forgot_password</item>
    </style>

</resources>